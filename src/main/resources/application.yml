spring:
  jpa:
    show-sql: false
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:testDb
    username: sa
server:
    port: 8080

---
spring:
  config:
    activate:
      on-profile:
      - dev